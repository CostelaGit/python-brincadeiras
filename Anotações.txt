list comprenhesion in python

v = [função ternaria] *isso será incluido em uma lista e colocado na
                       variavel v


! map e filter


a = list(filter(função, lista)) ! me retorna um filtro da lista original
a = list(map(função, lista))    ! me retorna uma nova lista
                                  de acordo com a função aplicada


list comprenhesion * pode ser aplicado a DICIONARIOs e a SETs